from ips.CreateIPSDataModel.tool_validator import ToolValidator as CIDMValidator


class ToolValidator(CIDMValidator):
    """Class for validating a tool's parameter values and controlling
    the behavior of the tool's dialog."""
