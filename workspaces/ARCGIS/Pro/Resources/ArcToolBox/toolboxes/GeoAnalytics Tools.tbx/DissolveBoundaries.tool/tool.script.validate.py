import ga_server_dissolveboundaries


class ToolValidator(ga_server_dissolveboundaries.ToolValidator):
    """Class for validating a tool's parameter values and controlling
    the behavior of the tool's dialog."""
