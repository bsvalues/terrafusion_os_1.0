from ips.GenerateIndoorPositioningFile.tool_validator import ToolValidator as GIPFValidator


class ToolValidator(GIPFValidator):
    """Class for validating a tool's parameter values and controlling
    the behavior of the tool's dialog."""
