import ga_server_describedataset


class ToolValidator(ga_server_describedataset.ToolValidator):
    """Class for validating a tool's parameter values and controlling
    the behavior of the tool's dialog."""

