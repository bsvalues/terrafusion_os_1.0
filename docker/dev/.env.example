# TerraFusion local Docker dev placeholders.
# Copy to .env only for local developer use. Do not add real secrets.

COMPOSE_PROJECT_NAME=terrafusion-dev
TF_DEV_MODE=local
ASPNETCORE_ENVIRONMENT=Development
VITE_API_URL=http://localhost:5000
