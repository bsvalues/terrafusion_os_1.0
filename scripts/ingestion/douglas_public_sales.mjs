#!/usr/bin/env node
import {createHash} from 'node:crypto';
import {mkdir,readFile,writeFile} from 'node:fs/promises';
import {dirname,join} from 'node:path';

const COUNTY='Douglas',CODE='017',OFFICIAL='https://www.douglascountywa.gov';
const MODE='public_assessor_arcgis_parcel_sales_table';
const LAYER='https://gis.douglascountywa.gov/server/rest/services/Parcel_Sales_Export_Tool/MapServer/5';
const PAYLOAD='douglas-arcgis-parcel-sales-export-tool-table-5.json';
const FIELDS=['OBJECTID','ParcelNumber','DORCode','TotalAcres','Neighborhood','CurrentNeighborhood','HouseNumber','StreetName','City','Zip','SaleDate','OriginalSalePrice','AdjustedSalePrice','Parcels','DeedType','ExciseAffidavit','RecordingNumber','LocalQual','StateQual','SaleNote','ReviewNote','YearBuilt','Quality','Condition','Bedrooms','Bathrooms','ResidentialArea','LandMrktValueAtSale','CommercialValueAtSale','ResidentialValueAtSale','APMrktValue'];
const GENERATED_AT=process.argv[2]??new Date().toISOString();
const ROOT=process.argv[3]??'frontend/apps/os-shell/public/launch-data/washington';
const PAGE=2000;
function inv(c,m){if(!c)throw new Error(m)}
function rec(v){return typeof v==='object'&&v!==null&&!Array.isArray(v)}
function canon(v){if(v===null||typeof v==='boolean'||typeof v==='string'){const s=JSON.stringify(v);if(s!==undefined)return s}if(typeof v==='number'&&Number.isFinite(v))return JSON.stringify(v);if(Array.isArray(v))return`[${v.map(canon).join(',')}]`;if(rec(v))return`{${Object.keys(v).sort().map(k=>`${JSON.stringify(k)}:${canon(v[k])}`).join(',')}}`;throw new Error('non-json value')}
function sha(v){return createHash('sha256').update(canon(v)).digest('hex')}
function ns(v){const s=String(v??'').trim();return s||null}
function num(v){const n=Number(String(v??'').replace(/[$,\s]/g,''));return Number.isFinite(n)?n:null}
function money(v){const n=num(v);return n!==null&&n>0?n:null}
function date(v){if(typeof v==='number'){const d=new Date(v);inv(Number.isFinite(d.getTime()),'invalid Douglas epoch date');return d.toISOString().slice(0,10)}const s=String(v??'').trim();let m=/^(\d{1,2})\/(\d{1,2})\/(\d{4})$/.exec(s);if(m)return`${m[3]}-${m[1].padStart(2,'0')}-${m[2].padStart(2,'0')}`;m=/^(\d{4})-(\d{2})-(\d{2})$/.exec(s);if(m)return s;throw new Error(`bad Douglas date ${s}`)}
async function fj(url,label){const r=await fetch(url,{cache:'no-store',credentials:'omit',headers:{Accept:'application/json'},redirect:'error'});inv(r.ok,`${label} HTTP ${r.status}`);const v=await r.json();inv(!v.error,`${label} ArcGIS ${JSON.stringify(v.error)}`);return v}
function q(params){const u=new URL(`${LAYER}/query`);for(const[k,v]of Object.entries(params))u.searchParams.set(k,v);return u}
async function fetchSales(){const meta=await fj(new URL(`${LAYER}?f=json`),'metadata');const have=new Set((meta.fields??[]).map(f=>f.name));for(const f of FIELDS)inv(have.has(f),`missing ${f}`);const c=await fj(q({where:'1=1',returnCountOnly:'true',f:'json'}),'count');inv(Number.isInteger(c.count)&&c.count>0,'empty Douglas sales');const features=[];for(let o=0;o<c.count;o+=PAGE){const p=await fj(q({where:'1=1',outFields:FIELDS.join(','),returnGeometry:'false',orderByFields:'OBJECTID',resultOffset:String(o),resultRecordCount:String(PAGE),f:'json'}),`page ${o}`);inv(Array.isArray(p.features),'missing features');features.push(...p.features.map(f=>f.attributes))}inv(features.length===c.count,'page count mismatch');return{metadata:{currentVersion:meta.currentVersion??null,objectIdField:meta.objectIdField??null,maxRecordCount:meta.maxRecordCount??null,fields:meta.fields.filter(f=>FIELDS.includes(f.name)).map(f=>({name:f.name,type:f.type,alias:f.alias??null,length:f.length??null}))},count:c.count,features}}
function top(records){const counts=new Map();for(const r of records){const n=r.currentNeighborhoodCode??r.neighborhoodCode;if(n)counts.set(n,(counts.get(n)??0)+1)}return Object.fromEntries([...counts.entries()].sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0])).slice(0,25))}
function map(a,i,payloadHash,genDate){if(!rec(a))return null;const oid=a.OBJECTID,parcel=ns(a.ParcelNumber),salePrice=money(a.AdjustedSalePrice)??money(a.OriginalSalePrice);if(!Number.isInteger(oid)||!parcel||salePrice===null)return null;const saleDate=date(a.SaleDate);if(saleDate>genDate)return null;const localQual=ns(a.LocalQual),stateQual=ns(a.StateQual);const needs=(localQual!==null&&localQual.toUpperCase()!=='Y')||(stateQual!==null&&stateQual.toUpperCase()!=='Y')||ns(a.ReviewNote)!==null;const id=[CODE,oid,parcel,a.ExciseAffidavit,a.RecordingNumber,saleDate,salePrice].map(v=>String(v??'').trim()).join('|');return{saleId:`WA-${CODE}-${createHash('sha256').update(id).digest('hex').slice(0,32)}`,county:COUNTY,countyCode:CODE,parcelNumber:parcel,saleDate,saleYear:Number(saleDate.slice(0,4)),salePrice:money(a.OriginalSalePrice)??salePrice,adjustedSalePrice:money(a.AdjustedSalePrice),documentNumber:ns(a.RecordingNumber)??ns(a.ExciseAffidavit),deedType:ns(a.DeedType),situsAddress:[ns(a.HouseNumber),ns(a.StreetName)].filter(Boolean).join(' ')||null,situsCity:ns(a.City),situsZip:ns(a.Zip),useCode:ns(a.DORCode),acres:num(a.TotalAcres),grantor:null,grantee:null,saleNote:needs?[ns(a.SaleNote),ns(a.ReviewNote),localQual?`LocalQual=${localQual}`:null,stateQual?`StateQual=${stateQual}`:null].filter(Boolean).join('; '):ns(a.SaleNote),neighborhoodCode:ns(a.Neighborhood),currentNeighborhoodCode:ns(a.CurrentNeighborhood),sourceMode:MODE,candidateSource:'douglas-official-arcgis-parcel-sales-table-5',confidenceScore:needs?0.85:1,qualityScore:needs?0.85:1,qualityBand:needs?'official_assessor_sale_needs_review':'official_assessor_sale',reviewStatus:needs?'needs_review':'ready',grossLivingArea:num(a.ResidentialArea),lotSizeSqft:null,yearBuilt:num(a.YearBuilt),bedrooms:num(a.Bedrooms),bathrooms:num(a.Bathrooms),condition:ns(a.Condition),qualityGrade:ns(a.Quality),provenance:{sourceUrl:LAYER,sourceFinalUrl:LAYER,sourcePayloadPath:PAYLOAD,sourcePayloadSha256:payloadHash,candidateIndexSource:`${PAYLOAD}#OBJECTID:${oid}`,candidateRecordType:'official-arcgis-assessor-sale',candidateSourceOrdinal:i,componentRows:[{sourceKey:'table-5',sourceUrl:LAYER,sourcePayloadPath:PAYLOAD,sourcePayloadSha256:payloadHash,candidateIndexSource:`${PAYLOAD}#OBJECTID:${oid}`}]},flags:{duplicateRisk:false,needsReview:needs,futureSaleDate:false,manualException:false}}}
async function rj(p){return JSON.parse(await readFile(p,'utf8'))}
async function wj(p,v){await mkdir(dirname(p),{recursive:true});await writeFile(p,`${JSON.stringify(v)}\n`,'utf8')}
function gen(v,g){if(Array.isArray(v))return v.map(e=>gen(e,g));if(rec(v)){const n={};for(const[k,e]of Object.entries(v))n[k]=k==='generatedAt'?g:gen(e,g);return n}return v}
async function main(){inv(new Date(GENERATED_AT).toISOString()===GENERATED_AT,'bad generatedAt');const genDate=GENERATED_AT.slice(0,10),payload=await fetchSales(),payloadHash=sha(payload);const records=payload.features.map((a,i)=>map(a,i+1,payloadHash,genDate)).filter(Boolean).sort((l,r)=>r.saleDate.localeCompare(l.saleDate)||l.saleId.localeCompare(r.saleId));inv(records.length>0,'no stageable Douglas sales');const ids=new Set();for(const record of records){inv(!ids.has(record.saleId),`duplicate ${record.saleId}`);ids.add(record.saleId)}const review=records.filter(r=>r.flags.needsReview).length,latest=records[0].saleDate,salesRoute=`/launch-data/washington/sales/by-county/${CODE}.json`,detailRoute=`/launch-data/washington/counties/${CODE}.json`;const shard={schemaVersion:'terrafusion.washington.sales-shard.v1',generatedAt:GENERATED_AT,county:COUNTY,countyCode:CODE,summary:{records:records.length,latestSaleDate:latest,reviewRecords:review,recordsWithNeighborhoodCode:records.filter(r=>r.currentNeighborhoodCode||r.neighborhoodCode).length,topNeighborhoodCodes:top(records)},records};const detail={schemaVersion:'terrafusion.washington.county-detail.v1',generatedAt:GENERATED_AT,county:COUNTY,countyCode:CODE,operationalState:{primarySourceMode:MODE,prometheusStatus:review>0?'needs_review':'public_data_ready'},summary:{records:records.length,latestSaleDate:latest},salesRoute};const statusEntry={county:COUNTY,countyCode:CODE,priority:'washington_assessor_launch',prometheusStatus:review>0?'needs_review':'public_data_ready',primarySourceMode:MODE,latestSaleDate:latest,candidateSales:payload.count,stagedSales:records.length,needsReview:review,confidence:{averageQualityScore:records.reduce((s,r)=>s+r.qualityScore,0)/records.length,parserStatus:'ready',rawStatus:'official_arcgis_parcel_sales_table_verified',rawDriftDetected:false},staticRoutes:{detail:detailRoute,salesShard:salesRoute}};const attest={algorithm:'SHA-256',canonicalJsonSha256:sha(shard),county:COUNTY,countyCode:CODE,officialSourceBaseUrl:OFFICIAL,route:salesRoute,sourcePayloadSha256:[payloadHash],sourcePosture:MODE};const manifestPath=join(ROOT,'manifest.json'),statusPath=join(ROOT,'counties','status.json');const manifest=gen(await rj(manifestPath),GENERATED_AT),status=gen(await rj(statusPath),GENERATED_AT);const keptAtt=manifest.salesShardAttestations.filter(e=>e.countyCode!==CODE),keptStatus=status.counties.filter(e=>e.countyCode!==CODE),shards=new Map();for(const a of keptAtt){const sp=join(ROOT,'sales','by-county',`${a.countyCode}.json`),dp=join(ROOT,'counties',`${a.countyCode}.json`),s=gen(await rj(sp),GENERATED_AT);await wj(sp,s);await wj(dp,gen(await rj(dp),GENERATED_AT));a.canonicalJsonSha256=sha(s);shards.set(a.countyCode,s)}shards.set(CODE,shard);status.generatedAt=GENERATED_AT;status.sourcePosture='mixed_public_assessor_sources';status.counties=[...keptStatus,statusEntry].sort((a,b)=>a.countyCode.localeCompare(b.countyCode));manifest.generatedAt=GENERATED_AT;manifest.sourcePosture=status.sourcePosture;manifest.statusCanonicalJsonSha256=sha(status);manifest.salesShardAttestations=[...keptAtt,attest].sort((a,b)=>a.countyCode.localeCompare(b.countyCode));manifest.summary={counties:status.counties.length,rawLanded:status.counties.length,parserReady:status.counties.filter(c=>c.confidence?.parserStatus==='ready').length,candidateSales:status.counties.reduce((t,c)=>t+c.candidateSales,0),stagedSales:status.counties.reduce((t,c)=>t+c.stagedSales,0),needsReview:status.counties.reduce((t,c)=>t+c.needsReview,0),prometheusNeedsReview:status.counties.filter(c=>c.prometheusStatus==='needs_review').length,recordsWithNeighborhoodCode:[...shards.values()].reduce((t,s)=>t+s.summary.recordsWithNeighborhoodCode,0),futureSaleDateRecords:[...shards.values()].reduce((t,s)=>t+s.records.filter(r=>r.flags?.futureSaleDate===true).length,0),criticalContradictions:0,garfieldExceptions:0,bentonCityAsNeighborhoodRecords:0};await wj(join(ROOT,'sales','by-county',`${CODE}.json`),shard);await wj(join(ROOT,'counties',`${CODE}.json`),detail);await wj(statusPath,status);await wj(manifestPath,manifest);await wj(join(ROOT,'receipts','douglas-source.json'),{schemaVersion:'terrafusion.washington.public-source-receipt.v1',county:COUNTY,countyCode:CODE,generatedAt:GENERATED_AT,sourceUrl:LAYER,sourcePayloadPath:PAYLOAD,sourcePayloadRecords:payload.count,sourcePayloadSha256:payloadHash,candidateSales:payload.count,stagedSales:records.length,quarantinedSales:payload.count-records.length,needsReview:review,omittedFields:['owner','grantor','grantee','buyer','seller','geometry']});console.log(JSON.stringify({county:COUNTY,countyCode:CODE,sourcePayloadSha256:payloadHash,candidateRecords:payload.count,stagedRecords:records.length,quarantinedRecords:payload.count-records.length,reviewRecords:review,latestSaleDate:latest,manifestCanonicalJsonSha256:sha(manifest)},null,2))}
await main();
