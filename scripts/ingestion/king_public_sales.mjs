#!/usr/bin/env node
import {createHash} from 'node:crypto';
import {mkdir,readFile,writeFile} from 'node:fs/promises';
import {dirname,join} from 'node:path';

const COUNTY='King',CODE='033',OFFICIAL='https://kingcounty.gov';
const MODE='public_assessor_arcgis_recent_sales_layer';
const LAYER='https://services.arcgis.com/Ej0PsM5Aw677QF1W/arcgis/rest/services/PARCEL_SALES3YR_AREA_287/FeatureServer/0';
const PAYLOAD='king-arcgis-parcel-sales-last-3-years-feature-0.json';
const FIELDS=['OBJECTID','PIN','address','ExciseTaxNum','SaleDate','SalePrice','RecNumber','Property_Type','Principal_Use','Property_Class'];
const GENERATED_AT=process.argv[2]??new Date().toISOString();
const MIN_SALE_DATE='2026-01-01';
const ROOT=process.argv[3]??'frontend/apps/os-shell/public/launch-data/washington';
const PAGE=1000;

function inv(c,m){if(!c)throw new Error(m)}
function rec(v){return typeof v==='object'&&v!==null&&!Array.isArray(v)}
function canon(v){if(v===null||typeof v==='boolean'||typeof v==='string'){const s=JSON.stringify(v);if(s!==undefined)return s}if(typeof v==='number'&&Number.isFinite(v))return JSON.stringify(v);if(Array.isArray(v))return`[${v.map(canon).join(',')}]`;if(rec(v))return`{${Object.keys(v).sort().map(k=>`${JSON.stringify(k)}:${canon(v[k])}`).join(',')}}`;throw new Error('non-json value')}
function sha(v){return createHash('sha256').update(canon(v)).digest('hex')}
function ns(v){const s=String(v??'').trim();return s||null}
function money(v){const n=Number(String(v??'').replace(/[$,\s]/g,''));return Number.isFinite(n)&&n>0?n:null}
function date(v){if(typeof v==='number'){const d=new Date(v);inv(Number.isFinite(d.getTime()),'invalid King epoch date');return d.toISOString().slice(0,10)}const s=String(v??'').trim(),m=/^(\d{4})-(\d{2})-(\d{2})$/.exec(s);inv(m,'bad King date');return s}
async function fj(url,label){const r=await fetch(url,{cache:'no-store',credentials:'omit',headers:{Accept:'application/json'},redirect:'error'});inv(r.ok,`${label} HTTP ${r.status}`);const v=await r.json();inv(!v.error,`${label} ArcGIS ${JSON.stringify(v.error)}`);return v}
function q(params){const u=new URL(`${LAYER}/query`);for(const[k,v]of Object.entries(params))u.searchParams.set(k,v);return u}
async function fetchSales(){const meta=await fj(new URL(`${LAYER}?f=json`),'metadata');const have=new Set((meta.fields??[]).map(f=>f.name));for(const f of FIELDS)inv(have.has(f),`missing ${f}`);const c=await fj(q({where:'1=1',returnCountOnly:'true',f:'json'}),'count');inv(Number.isInteger(c.count)&&c.count>0,'empty King sales');const features=[];for(let o=0;o<c.count;o+=PAGE){const p=await fj(q({where:'1=1',outFields:FIELDS.join(','),returnGeometry:'false',orderByFields:'OBJECTID',resultOffset:String(o),resultRecordCount:String(PAGE),f:'json'}),`page ${o}`);inv(Array.isArray(p.features),'missing features');features.push(...p.features.map(f=>f.attributes))}inv(features.length===c.count,'page count mismatch');return{metadata:{currentVersion:meta.currentVersion??null,objectIdField:meta.objectIdField??null,maxRecordCount:meta.maxRecordCount??null,fields:meta.fields.filter(f=>FIELDS.includes(f.name)).map(f=>({name:f.name,type:f.type,alias:f.alias??null,length:f.length??null}))},count:c.count,features}}
function top(records){return{}}
function map(a,i,payloadHash,genDate){if(!rec(a))return null;const oid=a.OBJECTID,parcel=ns(a.PIN),salePrice=money(a.SalePrice);if(!Number.isInteger(oid)||!parcel||salePrice===null)return null;const saleDate=date(a.SaleDate);if(saleDate>genDate||saleDate<MIN_SALE_DATE)return null;const id=[CODE,oid,parcel,a.ExciseTaxNum,a.RecNumber,saleDate,salePrice].map(v=>String(v??'').trim()).join('|');return{saleId:`WA-${CODE}-${createHash('sha256').update(id).digest('hex').slice(0,32)}`,county:COUNTY,countyCode:CODE,parcelNumber:parcel,saleDate,saleYear:Number(saleDate.slice(0,4)),salePrice,adjustedSalePrice:null,documentNumber:ns(a.RecNumber)??ns(a.ExciseTaxNum),deedType:null,situsAddress:ns(a.address),situsCity:null,situsZip:null,useCode:ns(a.Principal_Use)??ns(a.Property_Type)??ns(a.Property_Class),acres:null,grantor:null,grantee:null,saleNote:null,neighborhoodCode:null,currentNeighborhoodCode:null,sourceMode:MODE,candidateSource:'king-official-arcgis-parcel-sales-last-3-years-current-year-stage',confidenceScore:1,qualityScore:1,qualityBand:'official_assessor_recent_sale',reviewStatus:'ready',grossLivingArea:null,lotSizeSqft:null,yearBuilt:null,bedrooms:null,bathrooms:null,condition:null,qualityGrade:null,provenance:{sourceUrl:LAYER,sourceFinalUrl:LAYER,sourcePayloadPath:PAYLOAD,sourcePayloadSha256:payloadHash,candidateIndexSource:`${PAYLOAD}#OBJECTID:${oid}`,candidateRecordType:'official-arcgis-assessor-sale',candidateSourceOrdinal:i,componentRows:[{sourceKey:'feature-0',sourceUrl:LAYER,sourcePayloadPath:PAYLOAD,sourcePayloadSha256:payloadHash,candidateIndexSource:`${PAYLOAD}#OBJECTID:${oid}`}]},flags:{duplicateRisk:false,needsReview:false,futureSaleDate:false,manualException:false}}}
async function rj(p){return JSON.parse(await readFile(p,'utf8'))}
async function wj(p,v){await mkdir(dirname(p),{recursive:true});await writeFile(p,`${JSON.stringify(v)}\n`,'utf8')}
function gen(v,g){if(Array.isArray(v))return v.map(e=>gen(e,g));if(rec(v)){const n={};for(const[k,e]of Object.entries(v))n[k]=k==='generatedAt'?g:gen(e,g);return n}return v}

async function main(){inv(new Date(GENERATED_AT).toISOString()===GENERATED_AT,'bad generatedAt');const genDate=GENERATED_AT.slice(0,10),payload=await fetchSales(),payloadHash=sha(payload);const records=payload.features.map((a,i)=>map(a,i+1,payloadHash,genDate)).filter(Boolean).sort((l,r)=>r.saleDate.localeCompare(l.saleDate)||l.saleId.localeCompare(r.saleId));inv(records.length>0,'no stageable King sales');const ids=new Set();for(const record of records){inv(!ids.has(record.saleId),`duplicate ${record.saleId}`);ids.add(record.saleId)}const review=0,latest=records[0].saleDate,salesRoute=`/launch-data/washington/sales/by-county/${CODE}.json`,detailRoute=`/launch-data/washington/counties/${CODE}.json`;const shard={schemaVersion:'terrafusion.washington.sales-shard.v1',generatedAt:GENERATED_AT,county:COUNTY,countyCode:CODE,summary:{records:records.length,latestSaleDate:latest,reviewRecords:review,recordsWithNeighborhoodCode:0,topNeighborhoodCodes:top(records)},records};const detail={schemaVersion:'terrafusion.washington.county-detail.v1',generatedAt:GENERATED_AT,county:COUNTY,countyCode:CODE,operationalState:{primarySourceMode:MODE,prometheusStatus:'public_data_ready'},summary:{records:records.length,latestSaleDate:latest},salesRoute};const statusEntry={county:COUNTY,countyCode:CODE,priority:'washington_assessor_launch',prometheusStatus:'public_data_ready',primarySourceMode:MODE,latestSaleDate:latest,candidateSales:payload.count,stagedSales:records.length,needsReview:review,confidence:{averageQualityScore:1,parserStatus:'ready',rawStatus:'official_arcgis_current_year_recent_sales_layer_verified',rawDriftDetected:false},staticRoutes:{detail:detailRoute,salesShard:salesRoute}};const attest={algorithm:'SHA-256',canonicalJsonSha256:sha(shard),county:COUNTY,countyCode:CODE,officialSourceBaseUrl:OFFICIAL,route:salesRoute,sourcePayloadSha256:[payloadHash],sourcePosture:MODE};const manifestPath=join(ROOT,'manifest.json'),statusPath=join(ROOT,'counties','status.json');const manifest=gen(await rj(manifestPath),GENERATED_AT),status=gen(await rj(statusPath),GENERATED_AT);const keptAtt=manifest.salesShardAttestations.filter(e=>e.countyCode!==CODE),keptStatus=status.counties.filter(e=>e.countyCode!==CODE),shards=new Map();for(const a of keptAtt){const sp=join(ROOT,'sales','by-county',`${a.countyCode}.json`),dp=join(ROOT,'counties',`${a.countyCode}.json`),s=gen(await rj(sp),GENERATED_AT);await wj(sp,s);await wj(dp,gen(await rj(dp),GENERATED_AT));a.canonicalJsonSha256=sha(s);shards.set(a.countyCode,s)}shards.set(CODE,shard);status.generatedAt=GENERATED_AT;status.sourcePosture='mixed_public_assessor_sources';status.counties=[...keptStatus,statusEntry].sort((a,b)=>a.countyCode.localeCompare(b.countyCode));manifest.generatedAt=GENERATED_AT;manifest.sourcePosture=status.sourcePosture;manifest.statusCanonicalJsonSha256=sha(status);manifest.salesShardAttestations=[...keptAtt,attest].sort((a,b)=>a.countyCode.localeCompare(b.countyCode));manifest.summary={counties:status.counties.length,rawLanded:status.counties.length,parserReady:status.counties.filter(c=>c.confidence?.parserStatus==='ready').length,candidateSales:status.counties.reduce((t,c)=>t+c.candidateSales,0),stagedSales:status.counties.reduce((t,c)=>t+c.stagedSales,0),needsReview:status.counties.reduce((t,c)=>t+c.needsReview,0),prometheusNeedsReview:status.counties.filter(c=>c.prometheusStatus==='needs_review').length,recordsWithNeighborhoodCode:[...shards.values()].reduce((t,s)=>t+s.summary.recordsWithNeighborhoodCode,0),futureSaleDateRecords:[...shards.values()].reduce((t,s)=>t+s.records.filter(r=>r.flags?.futureSaleDate===true).length,0),criticalContradictions:0,garfieldExceptions:0,bentonCityAsNeighborhoodRecords:0};await wj(join(ROOT,'sales','by-county',`${CODE}.json`),shard);await wj(join(ROOT,'counties',`${CODE}.json`),detail);await wj(statusPath,status);await wj(manifestPath,manifest);await wj(join(ROOT,'receipts','king-source.json'),{schemaVersion:'terrafusion.washington.public-source-receipt.v1',county:COUNTY,countyCode:CODE,generatedAt:GENERATED_AT,sourceUrl:LAYER,sourcePayloadPath:PAYLOAD,sourcePayloadRecords:payload.count,sourcePayloadSha256:payloadHash,candidateSales:payload.count,stagedSales:records.length,quarantinedSales:payload.count-records.length,needsReview:review,stageWindow:{start:MIN_SALE_DATE,end:genDate},omittedFields:['owner','grantor','grantee','buyer','seller','sellername','selleradd1','selleradd2','buyername','buyeradd1','buyeradd2','geometry','Shape__Area','Shape__Length']});console.log(JSON.stringify({county:COUNTY,countyCode:CODE,sourcePayloadSha256:payloadHash,candidateRecords:payload.count,stagedRecords:records.length,quarantinedRecords:payload.count-records.length,reviewRecords:review,latestSaleDate:latest,manifestCanonicalJsonSha256:sha(manifest)},null,2))}
await main();
