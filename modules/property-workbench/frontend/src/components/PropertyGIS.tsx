import React from 'react';
import { TFCard, TFHeading, TFText } from '@terrafusion';

export const PropertyGIS: React.FC = () => {
  return (
    <TFCard>
      <TFHeading level={1} gradient>Property GIS</TFHeading>
      <TFText>Interactive property mapping and GIS tools coming soon...</TFText>
    </TFCard>
  );
};