name: Frontend CI (sanity-alt)

on:
  push:
    branches: [ feature/e2e-ci-strict ]
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Hello
        run: |
          echo "Alternate workflow parsed. Runner is alive."
          node -v || true
          npm -v || true
