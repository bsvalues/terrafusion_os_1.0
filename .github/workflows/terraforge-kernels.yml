name: CI • TerraForge Rust Kernels (cost + valuation)

# Builds and tests the two deterministic math kernels at packages/terrabuild/kernels.
# These kernels are invoked as subprocess JSON workers by the .NET backend's
# KernelValuationService (POST /api/valuation/kernel-cost-approach).
#
# Without this workflow, a deploy ships a .NET endpoint that 500s at subprocess
# launch because the binaries were never built. This workflow is the safety net.
#
# Trigger policy:
#   • push to main                      → always run + publish artifact
#   • PR touching kernels or workflow   → run, verify, no artifact publish
#   • workflow_dispatch                  → manual run
#
# Success gates:
#   1. cargo fmt --check passes (no formatting drift)
#   2. cargo clippy --all-targets -- -D warnings (zero warnings)
#   3. cargo test --workspace passes
#   4. Release binaries build for linux + windows targets
#   5. Binaries pass a smoke-test invocation (valid JSON in → valid JSON out)

on:
  push:
    branches: [main]
    paths:
      - 'packages/terrabuild/kernels/**'
      - '.github/workflows/terraforge-kernels.yml'
  pull_request:
    paths:
      - 'packages/terrabuild/kernels/**'
      - '.github/workflows/terraforge-kernels.yml'
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-test:
    name: Lint + test
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: packages/terrabuild/kernels
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust (stable, rustfmt + clippy)
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry + target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            packages/terrabuild/kernels/target
          key: kernels-${{ runner.os }}-${{ hashFiles('packages/terrabuild/kernels/Cargo.lock') }}
          restore-keys: |
            kernels-${{ runner.os }}-

      - name: cargo fmt --check
        run: cargo fmt --all -- --check

      - name: cargo clippy (zero warnings)
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: cargo test
        run: cargo test --workspace --locked

  build-release:
    name: Build release (${{ matrix.os }})
    needs: lint-test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            artifact-suffix: linux-x64
            bin-ext: ''
          - os: windows-latest
            artifact-suffix: windows-x64
            bin-ext: '.exe'
    defaults:
      run:
        working-directory: packages/terrabuild/kernels
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust (stable)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry + target (release)
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            packages/terrabuild/kernels/target
          key: kernels-release-${{ runner.os }}-${{ hashFiles('packages/terrabuild/kernels/Cargo.lock') }}
          restore-keys: |
            kernels-release-${{ runner.os }}-

      - name: cargo build --release --locked
        run: cargo build --release --workspace --locked

      - name: Smoke test — cost kernel returns success JSON for a known input
        shell: bash
        run: |
          COST_BIN="target/release/terraforge-kernel-cost${{ matrix.bin-ext }}"
          if [ ! -f "$COST_BIN" ]; then
            echo "::error::Missing expected binary at $COST_BIN"
            exit 1
          fi
          INPUT='{"contractPackVersion":"1.0.0","moduleApiVersion":"1.0.0","requestId":"ci-smoke-001","action":"calculate_cost","payload":{"subject":{"parcelId":"CI-0001","attributes":{"sqft":1850.0,"quality":"GOOD","condition":"AVERAGE"}},"tables":{"baseRate":145.50,"modifiers":{"GOOD":1.15,"AVERAGE":1.0,"DepreciationRate":0.10}}}}'
          OUTPUT=$(echo "$INPUT" | "$COST_BIN")
          echo "cost kernel stdout: $OUTPUT"
          echo "$OUTPUT" | grep -q '"success":true' || { echo "::error::cost kernel did not return success"; exit 1; }
          echo "$OUTPUT" | grep -q '"replacementCost":309551' || { echo "::error::cost kernel produced unexpected replacementCost"; exit 1; }

      - name: Smoke test — valuation kernel returns success JSON
        shell: bash
        run: |
          VAL_BIN="target/release/terraforge-kernel-valuation${{ matrix.bin-ext }}"
          if [ ! -f "$VAL_BIN" ]; then
            echo "::error::Missing expected binary at $VAL_BIN"
            exit 1
          fi
          INPUT='{"contractPackVersion":"1.0.0","moduleApiVersion":"1.0.0","requestId":"ci-smoke-002","action":"valuate","payload":{"subject":{"parcelId":"CI-0001","attributes":{}},"costBreakdown":{"replacementCost":309551.25,"depreciation":30955.125,"rcnld":278596.125},"model":{"landValue":65000.0,"adjustmentFactors":{"neighborhood":1.05,"location":0.98}}}}'
          OUTPUT=$(echo "$INPUT" | "$VAL_BIN")
          echo "valuation kernel stdout: $OUTPUT"
          echo "$OUTPUT" | grep -q '"success":true' || { echo "::error::valuation kernel did not return success"; exit 1; }

      - name: Upload release binaries (main branch only)
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: terraforge-kernels-${{ matrix.artifact-suffix }}
          path: |
            packages/terrabuild/kernels/target/release/terraforge-kernel-cost${{ matrix.bin-ext }}
            packages/terrabuild/kernels/target/release/terraforge-kernel-valuation${{ matrix.bin-ext }}
          retention-days: 30
          if-no-files-found: error
