name: core-governance-gates

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
  push:
    branches: [main]

env:
  TF_SECURITY_SCAN: '1'
  TF_PERF_BENCH: '1'
  TF_AI_SWARM_MONITOR: '1'
  TF_GOV_COMPLIANCE: '1'
  TF_ENFORCE_BRANCH_PROTECTION: '0'
  TF_REQUIRED_CHECKS: '🔒 SEAL,typecheck-core,phase83-tools'

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      core: ${{ steps.filter.outputs.core }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            core:
              - 'os-platform/core/**'
              - 'os-platform/core/fixtures/**'
              - 'tools/registry/**'
              - 'tools/registry/check-generated-js.mjs'
              - 'tsconfig.core.json'
              - 'package.json'
              - 'pnpm-lock.yaml'
              - '.github/workflows/**'

  phase-lane-server-guard:
    needs: changes
    if: needs.changes.outputs.core == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: CI Phase Lane Guard (no core touches on phase lanes)
        env:
          GITHUB_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          GITHUB_HEAD_REF: ${{ github.head_ref }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: bash scripts/ci/run-recorded-step.sh "phase-lane-server-guard" -- bash scripts/ci/phase-lane-server-guard.sh
      - name: CI Assert Clean Tree (no mutation)
        run: bash scripts/ci/run-recorded-step.sh "assert-clean-tree" -- bash scripts/ci/assert-clean-tree.sh
      - name: Collect failure diagnostics
        if: failure()
        shell: bash
        env:
          TF_DIAG_DIR: .tf-ci-diagnostics
        run: |
          mkdir -p .tf-ci-diagnostics
          git status --porcelain > .tf-ci-diagnostics/git-status.txt || true
          git diff --name-status > .tf-ci-diagnostics/diff-name-status.txt || true
          git diff --cached --name-status > .tf-ci-diagnostics/diff-cached-name-status.txt || true
          if [[ -f .tf-ci-diagnostics/audit-no-write.log ]]; then :; else echo "audit-no-write log unavailable for this step" > .tf-ci-diagnostics/audit-no-write.log; fi
          bash scripts/ci/write-failure-summary.sh
      - name: Upload failure diagnostics artifact
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: tf-governance-failure-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .tf-ci-diagnostics/
          if-no-files-found: error

  phase50-4-governance:
    needs: [changes, phase-lane-server-guard]
    if: needs.changes.outputs.core == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Audit governed surfaces are check-only
        shell: bash
        run: |
          set -euo pipefail
          record_fail() {
            code=$?
            mkdir -p .tf-ci-failure
            echo "audit-governed-no-write" > .tf-ci-failure/failing_step.txt
            echo "${code}" > .tf-ci-failure/failing_exit_code.txt
            if [[ -n "${GITHUB_ENV:-}" ]]; then
              {
                echo "TF_FAILING_STEP=audit-governed-no-write"
                echo "TF_FAILING_EXIT_CODE=${code}"
              } >> "${GITHUB_ENV}"
            fi
            exit "${code}"
          }
          trap 'record_fail' ERR
          mkdir -p .tf-ci-diagnostics
          exec > >(tee .tf-ci-diagnostics/audit-no-write.log) 2>&1
          echo "phase50.4: auditing governed surfaces for ungated write paths"
          surfaces=(.github/workflows/core-governance-gates.yml .husky/pre-push)
          forbidden='--write|prettier[[:space:]].*--write|eslint[[:space:]].*--fix([^a-zA-Z-]|$)|stylelint[[:space:]].*--fix|patch-package|changeset[[:space:]]+version|npm[[:space:]]+version'
          hits="$(rg -n -S -e "$forbidden" "${surfaces[@]}" || true)"
          # allow explicitly gated writes only
          ungated="$(echo "$hits" | grep -Ev 'TF_ALLOW_WRITE=1|--fix-dry-run' || true)"
          if [[ -n "${ungated}" ]]; then
            echo "ERROR: ungated write paths detected in governed surfaces:"
            echo "${ungated}"
            exit 1
          fi
          trap - ERR
          echo "phase50.4: no-write audit PASS"
      - name: Verify branch protection matches canon (optional enforce)
        shell: bash
        env:
          TF_REPO: ${{ github.repository }}
          TF_PROTECTED_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          record_fail() {
            code=$?
            mkdir -p .tf-ci-failure
            echo "verify-branch-protection" > .tf-ci-failure/failing_step.txt
            echo "${code}" > .tf-ci-failure/failing_exit_code.txt
            if [[ -n "${GITHUB_ENV:-}" ]]; then
              {
                echo "TF_FAILING_STEP=verify-branch-protection"
                echo "TF_FAILING_EXIT_CODE=${code}"
              } >> "${GITHUB_ENV}"
            fi
            exit "${code}"
          }
          trap 'record_fail' ERR
          echo "phase50.4: verifying branch protection for ${TF_REPO}@${TF_PROTECTED_BRANCH}"
          api="https://api.github.com/repos/${TF_REPO}/branches/${TF_PROTECTED_BRANCH}/protection"
          if ! json="$(curl -fsSL -H "Authorization: Bearer ${GITHUB_TOKEN}" -H "Accept: application/vnd.github+json" "$api" 2>/dev/null)"; then
            if [[ "${TF_ENFORCE_BRANCH_PROTECTION}" == "1" ]]; then
              echo "ERROR: failed to fetch branch protection and enforcement is enabled"
              exit 1
            fi
            echo "phase50.4: branch protection check skipped (insufficient token permissions)"
            exit 0
          fi
          contexts="$(echo "$json" | jq -r '.required_status_checks.contexts[]? // empty')"
          missing=0
          IFS=',' read -ra required <<< "${TF_REQUIRED_CHECKS}"
          for req in "${required[@]}"; do
            req="$(echo "$req" | xargs)"
            [[ -z "$req" ]] && continue
            if ! echo "$contexts" | grep -Fxq "$req"; then
              echo "ERROR: missing required status check: $req"
              missing=1
            fi
          done
          if [[ "$missing" == "1" ]]; then
            echo "Observed required checks:"
            echo "${contexts:-<none>}" | sed 's/^/  - /'
            exit 1
          fi
          trap - ERR
          echo "phase50.4: branch protection verification PASS"
      - name: Merge choreography gate (phase branches)
        shell: bash
        env:
          TARGET_BRANCH: main
        run: |
          set -euo pipefail
          record_fail() {
            code=$?
            mkdir -p .tf-ci-failure
            echo "merge-choreography" > .tf-ci-failure/failing_step.txt
            echo "${code}" > .tf-ci-failure/failing_exit_code.txt
            if [[ -n "${GITHUB_ENV:-}" ]]; then
              {
                echo "TF_FAILING_STEP=merge-choreography"
                echo "TF_FAILING_EXIT_CODE=${code}"
              } >> "${GITHUB_ENV}"
            fi
            exit "${code}"
          }
          trap 'record_fail' ERR
          branch="${GITHUB_HEAD_REF:-${GITHUB_REF_NAME:-}}"
          echo "phase50.4: merge choreography branch='${branch}' target='${TARGET_BRANCH}'"
          if [[ "${branch}" != feat/phase* ]]; then
            echo "phase50.4: not a phase lane; skip merge choreography"
            exit 0
          fi
          git fetch -q origin "${TARGET_BRANCH}:${TARGET_BRANCH}" || true
          if git merge-base --is-ancestor "origin/${TARGET_BRANCH}" HEAD; then
            echo "phase50.4: up-to-date with origin/${TARGET_BRANCH}"
          else
            echo "ERROR: branch is not up-to-date with origin/${TARGET_BRANCH}"
            echo "Run: git fetch origin ${TARGET_BRANCH} && git merge --no-ff origin/${TARGET_BRANCH}"
            exit 1
          fi
          trap - ERR
      - name: CI Assert Clean Tree (no mutation)
        run: bash scripts/ci/run-recorded-step.sh "assert-clean-tree" -- bash scripts/ci/assert-clean-tree.sh
      - name: Collect failure diagnostics
        if: failure()
        shell: bash
        env:
          TF_DIAG_DIR: .tf-ci-diagnostics
        run: |
          mkdir -p .tf-ci-diagnostics
          git status --porcelain > .tf-ci-diagnostics/git-status.txt || true
          git diff --name-status > .tf-ci-diagnostics/diff-name-status.txt || true
          git diff --cached --name-status > .tf-ci-diagnostics/diff-cached-name-status.txt || true
          if [[ -f .tf-ci-diagnostics/audit-no-write.log ]]; then :; else echo "audit-no-write log unavailable for this step" > .tf-ci-diagnostics/audit-no-write.log; fi
          bash scripts/ci/write-failure-summary.sh
      - name: Upload failure diagnostics artifact
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: tf-governance-failure-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .tf-ci-diagnostics/
          if-no-files-found: error

  phase50-5-main-invariants:
    if: github.event_name == 'push' && github.ref_name == 'main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Audit governed surfaces are check-only
        shell: bash
        run: |
          set -euo pipefail
          record_fail() {
            code=$?
            mkdir -p .tf-ci-failure
            echo "audit-governed-no-write" > .tf-ci-failure/failing_step.txt
            echo "${code}" > .tf-ci-failure/failing_exit_code.txt
            if [[ -n "${GITHUB_ENV:-}" ]]; then
              {
                echo "TF_FAILING_STEP=audit-governed-no-write"
                echo "TF_FAILING_EXIT_CODE=${code}"
              } >> "${GITHUB_ENV}"
            fi
            exit "${code}"
          }
          trap 'record_fail' ERR
          mkdir -p .tf-ci-diagnostics
          exec > >(tee .tf-ci-diagnostics/audit-no-write.log) 2>&1
          echo "phase50.5: auditing governed surfaces for ungated write paths"
          surfaces=(.github/workflows/core-governance-gates.yml .husky/pre-push)
          forbidden='--write|prettier[[:space:]].*--write|eslint[[:space:]].*--fix([^a-zA-Z-]|$)|stylelint[[:space:]].*--fix|patch-package|changeset[[:space:]]+version|npm[[:space:]]+version'
          hits="$(rg -n -S -e "$forbidden" "${surfaces[@]}" || true)"
          ungated="$(echo "$hits" | grep -Ev 'TF_ALLOW_WRITE=1|--fix-dry-run' || true)"
          if [[ -n "${ungated}" ]]; then
            echo "ERROR: ungated write paths detected in governed surfaces:"
            echo "${ungated}"
            exit 1
          fi
          trap - ERR
          echo "phase50.5: no-write audit PASS"
      - name: Verify branch protection matches canon (optional enforce)
        shell: bash
        env:
          TF_REPO: ${{ github.repository }}
          TF_PROTECTED_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          record_fail() {
            code=$?
            mkdir -p .tf-ci-failure
            echo "verify-branch-protection" > .tf-ci-failure/failing_step.txt
            echo "${code}" > .tf-ci-failure/failing_exit_code.txt
            if [[ -n "${GITHUB_ENV:-}" ]]; then
              {
                echo "TF_FAILING_STEP=verify-branch-protection"
                echo "TF_FAILING_EXIT_CODE=${code}"
              } >> "${GITHUB_ENV}"
            fi
            exit "${code}"
          }
          trap 'record_fail' ERR
          echo "phase50.5: verifying branch protection for ${TF_REPO}@${TF_PROTECTED_BRANCH}"
          api="https://api.github.com/repos/${TF_REPO}/branches/${TF_PROTECTED_BRANCH}/protection"
          if ! json="$(curl -fsSL -H "Authorization: Bearer ${GITHUB_TOKEN}" -H "Accept: application/vnd.github+json" "$api" 2>/dev/null)"; then
            if [[ "${TF_ENFORCE_BRANCH_PROTECTION}" == "1" ]]; then
              echo "ERROR: failed to fetch branch protection and enforcement is enabled"
              exit 1
            fi
            echo "phase50.5: branch protection check skipped (insufficient token permissions)"
            exit 0
          fi
          contexts="$(echo "$json" | jq -r '.required_status_checks.contexts[]? // empty')"
          missing=0
          IFS=',' read -ra required <<< "${TF_REQUIRED_CHECKS}"
          for req in "${required[@]}"; do
            req="$(echo "$req" | xargs)"
            [[ -z "$req" ]] && continue
            if ! echo "$contexts" | grep -Fxq "$req"; then
              echo "ERROR: missing required status check: $req"
              missing=1
            fi
          done
          if [[ "$missing" == "1" ]]; then
            echo "Observed required checks:"
            echo "${contexts:-<none>}" | sed 's/^/  - /'
            exit 1
          fi
          trap - ERR
          echo "phase50.5: branch protection verification PASS"
      - name: Assert clean tree (no mutation)
        run: bash scripts/ci/run-recorded-step.sh "assert-clean-tree" -- bash scripts/ci/assert-clean-tree.sh
      - name: Collect failure diagnostics
        if: failure()
        shell: bash
        env:
          TF_DIAG_DIR: .tf-ci-diagnostics
        run: |
          mkdir -p .tf-ci-diagnostics
          git status --porcelain > .tf-ci-diagnostics/git-status.txt || true
          git diff --name-status > .tf-ci-diagnostics/diff-name-status.txt || true
          git diff --cached --name-status > .tf-ci-diagnostics/diff-cached-name-status.txt || true
          if [[ -f .tf-ci-diagnostics/audit-no-write.log ]]; then :; else echo "audit-no-write log unavailable for this step" > .tf-ci-diagnostics/audit-no-write.log; fi
          bash scripts/ci/write-failure-summary.sh
      - name: Upload failure diagnostics artifact
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: tf-governance-failure-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .tf-ci-diagnostics/
          if-no-files-found: error

  governed-spine:
    needs: [changes, phase-lane-server-guard, phase50-4-governance]
    if: needs.changes.outputs.core == 'true'
    runs-on: ${{ vars.BENTON_RUNNER == 'true' && fromJSON('["self-hosted", "benton", "linux-x64"]') || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
      - name: Install
        run: bash scripts/ci/run-recorded-step.sh "install" -- pnpm install --frozen-lockfile
      - name: Typecheck core
        id: typecheck_core
        run: bash scripts/ci/run-recorded-step.sh "typecheck-core" -- pnpm run type-check
      - name: Capture tsc --showConfig (core) on typecheck-core failure
        if: failure() && steps.typecheck_core.outcome == 'failure'
        run: |
          set -euo pipefail
          mkdir -p .tf-ci-diagnostics
          pnpm exec tsc --showConfig -p tsconfig.core.json > .tf-ci-diagnostics/tsc-showconfig-core.json
      - name: Core tests (phase83 + tsconfig contract)
        run: bash scripts/ci/run-recorded-step.sh "core-tests" -- node --test os-platform/core/tests/phase83-tools.test.mjs os-platform/core/tests/tsconfig-core-types.contract.test.mjs
      - name: CI Assert Clean Tree (no mutation)
        run: bash scripts/ci/run-recorded-step.sh "assert-clean-tree" -- bash scripts/ci/assert-clean-tree.sh
      - name: Collect failure diagnostics
        if: failure()
        shell: bash
        env:
          TF_DIAG_DIR: .tf-ci-diagnostics
        run: |
          mkdir -p .tf-ci-diagnostics
          git status --porcelain > .tf-ci-diagnostics/git-status.txt || true
          git diff --name-status > .tf-ci-diagnostics/diff-name-status.txt || true
          git diff --cached --name-status > .tf-ci-diagnostics/diff-cached-name-status.txt || true
          if [[ -f .tf-ci-diagnostics/audit-no-write.log ]]; then :; else echo "audit-no-write log unavailable for this step" > .tf-ci-diagnostics/audit-no-write.log; fi
          bash scripts/ci/write-failure-summary.sh
      - name: Upload failure diagnostics artifact
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: tf-governance-failure-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .tf-ci-diagnostics/
          if-no-files-found: error

  phase85-tools:
    needs: [changes, phase-lane-server-guard, phase50-4-governance]
    if: needs.changes.outputs.core == 'true'
    runs-on: ${{ vars.BENTON_RUNNER == 'true' && fromJSON('["self-hosted", "benton", "linux-x64"]') || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
      - name: Install
        run: bash scripts/ci/run-recorded-step.sh "install" -- pnpm install --frozen-lockfile
      - name: Phase 8.5 tool gates
        run: bash scripts/ci/run-recorded-step.sh "phase85-tools" -- node --test os-platform/core/tests/phase85-tools.test.mjs
      - name: CI Assert Clean Tree (no mutation)
        run: bash scripts/ci/run-recorded-step.sh "assert-clean-tree" -- bash scripts/ci/assert-clean-tree.sh
      - name: Collect failure diagnostics
        if: failure()
        shell: bash
        env:
          TF_DIAG_DIR: .tf-ci-diagnostics
        run: |
          mkdir -p .tf-ci-diagnostics
          git status --porcelain > .tf-ci-diagnostics/git-status.txt || true
          git diff --name-status > .tf-ci-diagnostics/diff-name-status.txt || true
          git diff --cached --name-status > .tf-ci-diagnostics/diff-cached-name-status.txt || true
          if [[ -f .tf-ci-diagnostics/audit-no-write.log ]]; then :; else echo "audit-no-write log unavailable for this step" > .tf-ci-diagnostics/audit-no-write.log; fi
          bash scripts/ci/write-failure-summary.sh
      - name: Upload failure diagnostics artifact
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: tf-governance-failure-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .tf-ci-diagnostics/
          if-no-files-found: error

  phase86-toolrunner:
    needs: [changes, phase-lane-server-guard, phase50-4-governance]
    if: needs.changes.outputs.core == 'true'
    runs-on: ${{ vars.BENTON_RUNNER == 'true' && fromJSON('["self-hosted", "benton", "linux-x64"]') || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
      - name: Install
        run: bash scripts/ci/run-recorded-step.sh "install" -- pnpm install --frozen-lockfile
      - name: Phase 8.6 ToolRunner gates
        run: bash scripts/ci/run-recorded-step.sh "phase86-toolrunner" -- node --test os-platform/core/tests/phase86-toolrunner.test.mjs
      - name: CI Assert Clean Tree (no mutation)
        run: bash scripts/ci/run-recorded-step.sh "assert-clean-tree" -- bash scripts/ci/assert-clean-tree.sh
      - name: Collect failure diagnostics
        if: failure()
        shell: bash
        env:
          TF_DIAG_DIR: .tf-ci-diagnostics
        run: |
          mkdir -p .tf-ci-diagnostics
          git status --porcelain > .tf-ci-diagnostics/git-status.txt || true
          git diff --name-status > .tf-ci-diagnostics/diff-name-status.txt || true
          git diff --cached --name-status > .tf-ci-diagnostics/diff-cached-name-status.txt || true
          if [[ -f .tf-ci-diagnostics/audit-no-write.log ]]; then :; else echo "audit-no-write log unavailable for this step" > .tf-ci-diagnostics/audit-no-write.log; fi
          bash scripts/ci/write-failure-summary.sh
      - name: Upload failure diagnostics artifact
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: tf-governance-failure-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .tf-ci-diagnostics/
          if-no-files-found: error

  check-generated-js:
    needs: [changes, phase-lane-server-guard, phase50-4-governance]
    if: needs.changes.outputs.core == 'true'
    runs-on: ${{ vars.BENTON_RUNNER == 'true' && fromJSON('["self-hosted", "benton", "linux-x64"]') || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
      - name: Install
        run: bash scripts/ci/run-recorded-step.sh "install" -- pnpm install --frozen-lockfile
      - name: Regenerate core JS
        run: bash scripts/ci/run-recorded-step.sh "build-core-js" -- pnpm run build:core-js
      - name: Generated JS matches repo
        run: bash scripts/ci/run-recorded-step.sh "git-diff-exit-code" -- git diff --exit-code
      - name: Generated JS in sync
        run: bash scripts/ci/run-recorded-step.sh "check-generated" -- pnpm run check:generated
      - name: CI Assert Clean Tree (no mutation)
        run: bash scripts/ci/run-recorded-step.sh "assert-clean-tree" -- bash scripts/ci/assert-clean-tree.sh
      - name: Collect failure diagnostics
        if: failure()
        shell: bash
        env:
          TF_DIAG_DIR: .tf-ci-diagnostics
        run: |
          mkdir -p .tf-ci-diagnostics
          git status --porcelain > .tf-ci-diagnostics/git-status.txt || true
          git diff --name-status > .tf-ci-diagnostics/diff-name-status.txt || true
          git diff --cached --name-status > .tf-ci-diagnostics/diff-cached-name-status.txt || true
          if [[ -f .tf-ci-diagnostics/audit-no-write.log ]]; then :; else echo "audit-no-write log unavailable for this step" > .tf-ci-diagnostics/audit-no-write.log; fi
          bash scripts/ci/write-failure-summary.sh
      - name: Upload failure diagnostics artifact
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: tf-governance-failure-${{ github.job }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: .tf-ci-diagnostics/
          if-no-files-found: error

  no-relevant-changes:
    needs: changes
    if: needs.changes.outputs.core != 'true'
    runs-on: ubuntu-latest
    steps:
      - run: echo "No core governance surface changes — gates skipped."
