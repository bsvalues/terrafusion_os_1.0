/**
 * Entry point for the TerraField Mobile application
 */

import { registerRootComponent } from "expo";
import App from "./src/App";

// Register the main app component as the root component
registerRootComponent(App);
