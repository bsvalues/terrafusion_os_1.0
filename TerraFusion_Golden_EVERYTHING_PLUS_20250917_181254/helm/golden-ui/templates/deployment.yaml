apiVersion: apps/v1
kind: Deployment
metadata:
  name: golden-ui
spec:
  replicas: { { .Values.replicaCount } }
  selector:
    matchLabels: { app: golden-ui }
  template:
    metadata:
      labels: { app: golden-ui }
    spec:
      containers:
        - name: golden-ui
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: "{{ .Values.image.pullPolicy }}"
          ports:
            - containerPort: 4000
          env:
            {% for e in .Values.env %}
            - name: "{{ e.name }}"
              value: "{{ e.value }}"
            {% endfor %}
          resources: { {- toYaml .Values.resources | nindent 12 } }
