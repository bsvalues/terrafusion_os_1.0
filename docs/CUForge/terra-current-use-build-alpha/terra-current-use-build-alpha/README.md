# Terra Current Use Build Alpha

First buildable alpha lane only.

Build path:

Property Workbench → Current Use tab → rollback calculation → explanation ledger → notice preview.

Everything else stays disabled until this compiles.
