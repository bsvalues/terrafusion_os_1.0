# Moved → `docs/brain/`

The solo-dev control plane evolved into the **TerraFusion Brain**. Everything that was here now
lives under `docs/brain/` with a richer structure (canon / memory / graph / agents / reports) plus a
runnable CLI.

- Brain overview & how to use: [`docs/brain/README.md`](../brain/README.md)
- Daily control note: [`docs/brain/00_TODAY.md`](../brain/00_TODAY.md)
- CLI: `pnpm brain status` · `pnpm brain ask "..."` · `pnpm brain classify "..."` · `pnpm brain check`

This folder is intentionally left as a redirect to avoid a stale second vault (which would itself be
the contradictory-docs drift the Brain exists to prevent).
