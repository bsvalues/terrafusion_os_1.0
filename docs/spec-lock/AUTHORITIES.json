{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "version": "2.0",
  "description": "SpecLock Authority Registry (GOD-TIER) - Federated multi-authority cryptographic governance",
  "quorum": 2,
  "federated_quorum": {
    "description": "At least one authority from required_authorities must sign",
    "required_authorities": ["benton_county"],
    "vendor_optional": false
  },
  "authorities": [
    {
      "id": "benton_county",
      "name": "Benton County, WA",
      "description": "Primary county authority for sovereign data governance",
      "public_key_path": "artifacts/speclock/keys/benton_county.pub",
      "kms": {
        "provider": "local",
        "key_ref": "artifacts/speclock/keys/benton_county.key",
        "notes": "For production, use: aws|azure|gcp with HSM-backed key"
      },
      "active": true,
      "valid_from": "2025-01-01T00:00:00Z",
      "valid_to": null
    },
    {
      "id": "vendor",
      "name": "TerraFusion Vendor",
      "description": "Vendor authority for code integrity",
      "public_key_path": "artifacts/speclock/keys/vendor.pub",
      "kms": {
        "provider": "local",
        "key_ref": "artifacts/speclock/keys/vendor.key",
        "notes": "For production, use: aws|azure|gcp with HSM-backed key"
      },
      "active": true,
      "valid_from": "2025-01-01T00:00:00Z",
      "valid_to": null
    }
  ],
  "rotation_policy": {
    "overlap_days": 30,
    "description": "New keys must overlap with old keys for at least 30 days before old key is deactivated"
  },
  "notes": [
    "No deletes. Ever. Mark valid_to to sunset a key.",
    "Rotation: add new key with active=true, keep old active during overlap, then set old valid_to",
    "KMS providers: local (dev), aws (awskms://), azure (azurekms://), gcp (gcpkms://)",
    "Quorum must be satisfied for any manifest to be trusted",
    "At least one required_authority must sign (federated constraint)"
  ]
}
