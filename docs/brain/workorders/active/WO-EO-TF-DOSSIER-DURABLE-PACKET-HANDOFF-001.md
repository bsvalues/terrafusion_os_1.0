Work Order: WO-EO-TF-DOSSIER-DURABLE-PACKET-HANDOFF-001
Owner EO: EO-TF-DOSSIER-DURABLE-PACKET-HANDOFF-001
Status: NOT_COMPLETE_PENDING_PROTECTED_OS_MERGE_AND_POSTMERGE_BROWSER_REPEAT; whole68 API/fixture/UI built and bound, independently reviewed060 harness actual78594 PASS, immediate409 stale-eligibility P2 closed. Final evidence/head review and coordinator-owned protected delivery/repeat remain required.
Base: ed442c84b916cb7a667cc162236dc9dae0b1b39b; branch codex/eo-dossier-os-001.
Coordinator owns review, protected lifecycle, shared registration and environment release.

## Current whole-candidate acceptance — actual78594 PASS; NOT_COMPLETE

Application build source: `68f2893f9d28639c0262cdf2646abe75b5a9ff8f`, after normal protected073 integration. Harness-only source: `060bfdde45a11aae9e78db730c5d68cb4ae758fa`. Whole scoped API graph rebuild95517 and fixture41384 exited0 with zero warnings/errors; normal changed frontend41757 exited0 (tsc --noEmit && Vite). Normal frontend success is not semantic-project PASS: existing noCheck:true and the separately recorded baseline diagnostic failures remain qualified. No fullsolution or production enablement.

Whole build binding verifies24 DLL/PDB pairs,4682 document records/2505 unique checksums (including protected073 PilotRuntimeProxy), and302 newly built UI assets. API DLL SHA256 `2d5ba0dfdfa80154756a3b76519105623e5e2c08dc4fa04adac27abcb3d821e0`; fixture DLL `a37b78b8228594785b413ced0b9e14678bc5dc03ff702d0c937d1acad281392d`; UIindex `2371583226dbaf3ec5e9cb1fa68f414fa242820afb02a4e2624d970f36949bd2`. These remain actual68 build artifacts, not falsely relabelled060 binaries. Product sources are identical at060; only the reviewed test harness differs.

Actual browser78594 started2026-09-08T07:13:23.258Z at8.988GiB free, naturalEXIT0:1PASS/0skip/0flaky, oneChromiumworker/zero browser retries,48.0s journey/64.837559s total. Fresh synthetic prerequisites only, real API5191, no response mocks or token-bearing HAR/trace/video. All20 authenticated property/context/screen preflights returned200. Both fixture seed and read-only verification executed1/passed1/skipped0. Narrative, seal, prepare, exact retry, reload, API restart, scope/auth/readiness refusal, stale refusal and revision-to-draft paths passed. Exactly4 immutable workflow records/4 corresponding audits, no appeal/link success writes.

The independently reviewed status-first409 P2 correction is closed by actual immediate-before-Reload assertions: current handoff and Dais action removed, all mutation controls disabled, truthful stale/reload-required state and historical receipts/failedCID retained. Both JSON and stalled-body cases also have21/21 committed focused UI regressions. Actual UI stale-revise evidence is observed HTTP409 plus request/header CID `0273a7fc-2f76-45d3-89fd-c9e55e38b115`,82.57ms through response headers, `responseBody=not-read-status-first-conflict`, `errorCode=null`; no invented error JSON. Existing direct stale-prepare separately parsed/asserted REVISION_CONFLICT; direct nonfinalized parsed/asserted NONFINALIZED. Successful bodies remain mandatory and exactly equal persisted receipts.

Actual finalization `84b4a120-90ab-484b-b7e2-7b6641e6089c`, handoff `b119bee4-475b-4b57-8f75-2812cccb313c`, packet revision `c39d2a23efa1131d156e4d6ac13e59ab48b7757272fefa6edc396749f8b144b3`. Finalize CID `3a1a1257-53cb-4c5d-aaec-fe7af2d17105`/1388.44ms and prepare CID `58993a42-ecf9-4689-9879-d372361975e2`/509.12ms are measured through parsed response bodies. Five browser actions join11 sanitized actual ILogger events; four successful CIDs join four persisted audits, failedCID has no success receipt. Screenshots retain actual prepared receipt/provenance, sealed/reopened state and failed historical state.

Local evidence run: `.tmp/dossier-packet-browser/run-d76f2d7fdd2245d5bf80e3a608ca3b49/`. Results/screenshots: `output/playwright/dossier-packet-workflow/candidate-060bfdde4-96b16a9840af4a9fa9567d5cc323b686/`. Build and afterhash directory: `.tmp/dossier-packet-browser/build-68f2893-7b7775d85f1449b8b38521ceabae1bc2/`. Afterhash78594PASS rechecks all24DLL/PDB pairs,302UIassets and seven existing stage manifests/module/schema sets; source remains frozen. Receipt `afterhash-78594-pass.json` SHA256 `eea5e5a910f38e6a2e6e4a5a7cc6f7a9372c63b568befe14f681776383f95c2c`. Initial afterhash helper incorrectly assumed Atlas had a schema file and refused; corrected verifier recognizes the existing module-only Atlas manifest, no runtime/artifact mutation.

Evidence SHA256: results.json `86a7f44ff4f46fcf80adf238e99f54940f90e00a2ebe787e68f281e14867414e`; persisted-evidence.json `4cc175f67d03634722be11eba3ea5129d84e9ee7132fb9e9102314f120c58bda`; observed-receipts.json `32e281f7823fc3bb02592296687177f85dbfda7cb45cbfb3ed5c11ce6438c667`; sanitized-service-trace.json `4f38df36c9bfb068c5d6f4d35777109fdd8eae79a8135aefead54a7a4b62f055`; final DB `b5464fa042a2f87d84c972ebecf4ebea9c54dcbf4a47b7ff0ebbb3ac00b99ab5`. Failed screenshot SHA `8b07d363dc33629cde795b375509fdcb766dd7b17655b7048d4e8e4a6ab61822`; prepared receipt screenshot `db95601f03369f1b417a9799d883d6fed78bdb74f3e47db8da2def53c06b2c64`.

All owned runtime processes naturally cleaned up and5191 listener absent; release free9.010GiB. No build/restart during afterhash. Old94380 testPASS remains historical with its subsequently discovered missing P2 assertion;88861 remains actual harnessFAIL and is preserved, not relabelled. Current candidate acceptance does not certify protected OS delivery: status is **NOT_COMPLETE_PENDING_PROTECTED_OS_MERGE_AND_POSTMERGE_BROWSER_REPEAT**. Independent final evidence/head review and coordinator-owned normal protected GitHub lifecycle precede delivered repeat. No builder push, invented PR number, production enablement or full EO completion claim.

## Historical candidate76 acceptance and integration checkpoint

Candidate: `76d11f73ea7269b28614bbda0787200f533dd133`, independently reviewed over51d503; no additional findings. Actual browser session94380 passed on2026-09-08 (start05:35:00.626Z),1worker/0retry, no trace/HAR/video, fresh synthetic prerequisites and actual API5191. Authenticated property/context and all eight mounted Dossier read preflights returned200 on initial/restart openings. UI saved narrative, finalized exact source revision, prepared handoff, displayed actual backend receipts/CIDs/provenance, and reopened identical durable state after reload and owned API restart. Exact retry returned the same receipt. Missing sources/narrative, wrong scope/auth, changed-source stale attempt, nonfinalized handoff and revision-to-draft assertions passed. Read-only verification found exactly four immutable workflow records/four corresponding workflow audits and no appeal/link writes.

Actual synthetic finalization `8ddf4746-0961-4875-9d74-8ec6a5de3263`, handoff `795ac2dd-5337-4813-a4bd-f17a3b3d7654`, packet revision `6ba0cbe27b4c323870a484f47598428048fe444aab76ac67364d6ff94bde01dc`. Finalize/prepare/stale-failure UI latencies1360.85/506.41/72.85ms; actual service logs, persisted audit rows and UI CIDs joined. Four explicit screenshots cover sealed state, prepared receipts/CIDs, reopened state and failed attempt CID. No manufactured successful receipts or mock API responses.

Independent acceptance finding (Halley, after source review): the actual failed-action screenshot shows authoritative409/REVISION_CONFLICT and failed CID while retaining old sealed/prepared state and an active Continue in Dais link until manual Reload. The existing browser assertion checks link removal only after Reload. Test PASS is truthful but does not close this stale-eligibility presentation P2; backend refusal/no unauthorized persistence remain proven. Exact panel/test/harness remediation proposed to coordinator; no frozen product change in this documentation checkpoint. Current handoff/action eligibility must retire on authoritative conflict while preserving historical receipts and failed-attempt CID, followed by a truthful current read. Independent successor and browser verification remain required.

Preserved local run: `.tmp/dossier-packet-browser/run-7c7d3d60982d4e3c86deafb64b842dfb/`; screenshots/results: `output/playwright/dossier-packet-workflow/candidate-76d11f73e-46009f1eaab1476986dc59f22547e184/`. Both fixture seed and read-only verification executed1/passed1/skipped0. Evidence SHA256: identity.json `fd06e2d978bb42d95d7c110aba7606f262ad8c02b6eb8a8a12478aaa0c4204f0`; persisted-evidence.json `be747d49e3d082ada4337a125369b00d05fa962fd2922b751e00d7ea818ce9b5`; sanitized-service-trace.json `87f0ccfe0ae2b65bd1208f5525c56f1fde67335229a49e13caae9fc1ccaa8318`. Artifacts are preserved locally, not committed or disguised as hosted evidence.

Candidate build binding: API DLL `ca11c31627ed83b5ebfff4ad2f9aba2ef2102afd1b476316873cd80f2fd00fc9`, Unit DLL `e63f345be8e90a0c31e6b23e5a24dec4fbc549628277c558d5fd1dd653e9d935`; relevant PDB source hashes equal tested/committed C# bytes after normal hooks. UI index `5db83186299787caccf3680546af8834fccd391f3e7a807023d1ebf74c3dd18c` is the unchanged earlier d3a build, not a new76d11 build. Normal frontend build passed historically, but explicit `--noCheck false --incremental false` diagnostic remained1585 baseline errors after three owned import fixes; no semantic-project PASS claimed.

Actual prerequisite REDs remain preserved: missing Valuations/CamaCharacteristics, then Context missing ValuationRecords. Source-backed empty EF table/index batch totals17 tables, with no seeded valuation/levy/note/workflow/appeal outcomes and eleven ordinary prerequisite autoaudits retained. Retained95059 DB proved EF Unspecified timestamp -> frozen adapter refusal -> controller500. Narrow SQLite-only normalization now preserves UTC ticks; two actual persist/reload regressions RED then50/50 scoped GREEN, same unchanged DB characterization500->200 with SHA256 `46ccf94169822c20b1043de75b91ae0e190178f6ef6fef59b2565d7819622956`. Local conversion, existing UTC and non-SQLite negative guards preserved; adapter/schema/pins unchanged.

Integration: fresh origin/main `0733406fe9067f5d5e61c4a361951c1c3700a9c8` merged normally as `ab97da59012411a8a7c1d71bcd094d6250fcd694`. Only overlap was additive `.gitignore` entries; protected Atlas and Dossier paths preserved. This merge adds other protected application/frontend changes. The76d11 browser PASS is NOT a build/browser proof of the merged integration tree. No new build/server ran in this checkpoint window. Coordinator owns normal push/PR/protected merge; integrated/delivered source-bound verification and actual post-merge browser repeat remain required. No EO COMPLETE or production enablement claim.

## Protected adoption identity

Suite PR8 protected main commit: 8f58a6b989641a6fde063afa3dda68bd18062c63. Exact Git commit and all four source/spec blobs verified; no local candidate identity substituted.
Portable aggregate SHA-256: d4f29a599c96499f567c065274127b5c6943955b6366bd5959166ac5abf55c01.
Exact suite EO/spec artifact: operations/work-orders/EO-TF-DOSSIER-DURABLE-PACKET-HANDOFF-001.md, 10579 bytes, fc3b630e41cb26c86f946cb0ca9b14e4b00084ae9844f632b6120627d2c15723.
All three transitive modules retain suite paths in .terrafusion/runtime/dossier/packet-workflow; generated manifest is 928 bytes, SHA-256 25f1a91faef5ad1867c058fc57795016ba20fdd090fb596c43255fa637bd159c.
Bounded Stage-DossierPacketWorkflowModule.ps1 uses the existing bootstrap/owned-slot seam. Actual local staging, candidate-tamper refusal and post-publish rollback to identical prior bytes passed. No application runtime enabled by staging.
New exchange transport remains API-owned DTO/private process JSON; no duplicate Abstractions class, frozen corpus or shared manifest change is required by current classification. Shared freeze remains coordinator-held. Below is the identical approved suite specification, not an OS rule implementation.

<!-- prettier-ignore-start -->

# Dossier packet workflow exchange specification 1.0.0

Authority: owner's EO-TF-DOSSIER-DURABLE-PACKET-HANDOFF-001; coordinator contract-authority.md and reservations.md (2026-09-07). This is the approved new capability specification, not protected-source or contract-mirror evidence. Carry verbatim into reserved OS WO-EO-TF-DOSSIER-DURABLE-PACKET-HANDOFF-001.md after checkout readiness. Frozen exchanges unchanged.

## Requests

All objects have closed fields. JSON strings are preserved except canonical evidence ordering. IDs below are nonempty UUID strings; countyId comparisons are exact canonical lower-case UUIDs. Hashes/revisions are lower-case 64-hex SHA-256 supplied by OS. TaxYear integer 1900..2200. Actor/trace/command identifiers nonblank strings, maximum 200 characters; parcelId nonblank maximum 50. Timestamps are valid UTC RFC3339 instants supplied by OS. No suite I/O/ID/time/hash allocation.

Common required keys: `schemaVersion` (literal `1.0.0`), `contractId`, `operation`, `commandId`, `countyId`, `taxYear`, `parcelId`, `actorId`, `effectiveAt`, `expectedRevision`, `traceId`, `hostAssertions`, `packet`, `template`, `currentDocuments`, `narrative`, `evidence`, `provenance`.

- `hostAssertions`: actorAuthorized, countyExists, parcelExists, piiApproved (all boolean true).
- `packet`: packetId, countyId, taxYear, parcelId, packetType, name, status (`draft|complete|sealed`), revision. packetType/name nonblank, max50/200 respectively.
- `template`: packetType, name, requiredDocumentTypes. Exactly the existing createPacket template semantics, nonempty requirement list for finalization.
- `currentDocuments`: existing createPacket document snapshots: documentId, countyId, parcelId, documentType, status (`active|sealed|archived`), uploadedAt; additionally taxYear, revision, contentHash for exact scoped evidence identity. Unique documentIds; maximum1000 records.
- `narrative`: content (string, max16000 characters), revision, contentHash. Empty content is a typed readiness blocker, not an input-shape failure.
- `evidence`: array(max1000) of { evidenceId, documentId? , revision, contentHash, countyId, taxYear, parcelId }; unique evidenceIds. A documentId must resolve to a nonarchived supplied document of identical scope/contentHash. Empty evidence is a typed readiness blocker.
- `provenance`: suiteCommit (40 lower-case hex), artifactSha256 (64 lower-case hex), contractVersion (`1.0.0`), traceId (matches request traceId). This attests only supplied provenance; OS independently verifies protected identity at staging/invocation and never accepts browser-provided provenance.

Packet revision hashes the OS canonical source serialization of packet scope/type/name, template, selected/current document metadata including state/revision/hash, narrative and evidence; excludes packet workflow status, operational timestamps and finalization/handoff/link receipt IDs so committing a seal does not invalidate itself. Reads/commands recapture sources within one transaction. Source modifications, including same-count swaps, change revision. Suite also compares full canonical snapshot to persisted finalization so reusing a claimed revision cannot hide changed source references.

Finalization contractId=`dossier.packet-finalization`; operation=`evaluate|finalize|revise`. Optional `finalization` is the persisted seal described below; `finalizationId` required for finalize, absent otherwise; `revisionReason` required for revise (nonblank max2000), absent otherwise. Revise reopens only an existing sealed packet and returns draft; it does not rewrite old finalization. A draft with an old seal is not handoff eligible.

Handoff contractId=`dossier.appeal-handoff`; operation=`prepare`. Additional required `handoffId`; optional `finalization` to allow typed NONFINALIZED refusal when missing. No finalizationId/revisionReason request fields.

## Decisions

Every result: schemaVersion, contractId, commandId, countyId, taxYear, parcelId, packetId, traceId, decision (`accepted|rejected`), status (`draft|complete|sealed|stale|prepared|failed`), violations ([{code,message}]). Only accepted finalization results may contain `snapshot`; only accepted handoff contains `handoff`; accepted evaluate/revise may contain `readiness` with completenessPercent/satisfiedCount/totalRequired/items. Rejected results never carry an authoritative snapshot/handoff. Malformed identity fields are omitted from rejection instead of echoed with wrong types.

Validation ordering: malformed/unknown/version input -> INVALID_INPUT; host assertions -> HOST_ASSERTION_FAILED; source/template identity -> IDENTITY_MISMATCH; expectedRevision mismatch -> STALE_REVISION; incomplete canonical template -> INCOMPLETE_PACKET; blank narrative -> MISSING_NARRATIVE; no evidence -> MISSING_EVIDENCE. Invalid/missing evidence links -> INVALID_EVIDENCE. Missing seal/draft current packet -> NONFINALIZED; differing persisted seal scope -> IDENTITY_MISMATCH; differing exact revision or snapshot sources -> STALE_FINALIZATION. Complete inputs only seal. Evaluate reports completeness/readiness and existing valid seal. Revise requires sealed packet and reason, expectedRevision current; returns draft, retaining prior record outside suite.

`snapshot` (persisted immutable finalization): schemaVersion=`1.0.0`, contractId=`dossier.packet-finalization`, finalizationId, countyId, taxYear, parcelId, packetId, packetType, name, packetRevision, status=`sealed`, finalizedAt, finalizedBy, narrative, evidence, items (exact accepted existing createPacket selection), template, currentDocuments, provenance. `template` retains all request template fields with requiredDocumentTypes in supplied order. `currentDocuments` retains every supplied document (including unselected/archived records) with all request document fields, sorted by ordinal documentId. Both are detached canonical objects, independent of input property order. Freshness compares these complete source snapshots as well as narrative/evidence/items, not merely selection IDs or the claimed packet revision. UTC timestamps admit zero to seven fractional digits; ordering compares whole seconds and the fraction padded to seven digits without millisecond truncation. Equivalent fractional widths compare equal. Finalization compares supplied prior snapshot if present; exact finalization retry with same id must match its stored source and metadata; changed commands are not idempotent. OS requestId/hash lookup owns exact retry receipt retrieval before allocating new IDs/time.

`handoff` (sole Dais envelope, same fields as initial approved report): schemaVersion=`1.0.0`, contractId=`dossier.appeal-handoff`, handoffId, countyId, taxYear, parcelId, packetId, packetType, packetRevision, finalizationId, finalizedAt, finalizedBy, narrative, evidence, preparedAt, preparedBy, provenance. Evidence order ascending evidenceId using ordinal comparison, no locale-dependent order. Nested data is detached from caller objects. Envelope uses persisted seal narrative/evidence and current request's verified provenance. Source scope/revision and full canonical content must match. No appeal write fields in requests/results.

## Integration

Suite exports `decideDossierPacketFinalization(request)` and `decideDossierAppealHandoff(request)`. Shared pure validation/snapshot helper exports may live in packet-finalization module; handoff imports it. Existing createPacket judgment imported from unchanged mutation-decision module. OS stage all required modules by exact protected commit/hash and use existing constrained Node process pattern. Do not duplicate semantic rules in OS.

Dais reads durable handoff via proposed Dossier-owned GetPreparedHandoffAsync(countyId,taxYear,parcelId,handoffId,expectedPacketRevision), then links existing scoped appeal using LinkAppealAsync(countyId,taxYear,parcelId,handoffId,expectedPacketRevision,appealId,requestId). Durable content hash wraps serialized envelope; no self-hashing field. Exact protected Dossier identity binds final adoption; proposed/synthetic provenance is never proof of protection.

## TDD examples

Synthetic only: complete one-required-document packet with matching evidence/narrative seals exact revision; missing requirements/narrative/evidence do not seal; wrong scope and unknown cross-suite fields reject; swapped evidence ID/hash with same count and same supplied revision invalidates old seal; revised draft refuses handoff; canonical order and fixed host IDs/time/provenance produce identical serialized envelope. Immutable input and output isolation tested. Persistence/browser proof remains required after suite/OS protection.

<!-- prettier-ignore-end -->
