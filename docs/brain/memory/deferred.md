# Deferred Until After 1.0

> The anti-expansion safety valve. This is **not a graveyard** — it is where good ideas wait so
> they cannot hijack the release. When you feel the pull to "also build X," put X here and return
> to the work order.

## Hard-deferred (do not start before 1.0 ships)
- New county-office suites of any kind
- **TerraClerk** (reserved)
- **TerraTreasury** (reserved)
- **TerraAudit** (reserved)
- **TerraRecorder** (reserved)
- Marketplace monetization / plugin revenue expansion
- Autonomous multi-agent production layer
- Statewide / multi-county interoperability
- Full self-healing OS
- Advanced AI swarm expansion (the 1,008-agent swarm stays frozen — do not modify)
- Complete ontology / knowledge-graph rebuild

## Soft-deferred (P2/P3 — only if cheap and adjacent to authorized work)
- Demo-script polish
- UI visual polish beyond honesty labels
- Test-coverage expansion beyond release-critical paths
- Documentation beyond canon + release evidence

## Parked ideas (date them; revisit at release-evidence time)
| Date | Idea | Why deferred |
|------|------|--------------|
| 2026-06-09 | **"Cortex Prime" Prometheus-grade engine fleet** — what-if *simulation* (beyond the deterministic `what-if` judge), `trial`, `council` (multi-perspective), `red-team` adversarial agent, `memory court`, `black-box` incident replay, `apply-patch` execution membrane, full ontology schema + relationship queries, agent scorecard automation, public-wiki export | Scope inflation. Your own locked doctrine ([[project_benton_truth_singular_gate]]) forbids "Prometheus-grade" framing until a full-corpus evidence artifact exists; and the standing failure mode is building speculative engines instead of the concrete next thing. Revisit **after 1.0 ships** — each engine becomes its own work order only when a concrete need proves it. |
| 2026-06-09 | Public/external wiki tier (filtered subset of `wiki/`) | Internal wiki is the priority; public export is a filter step, post-1.0. |

**Rule:** moving an item *out* of Deferred requires it to become a row in [[release-gates]] or a
work order in [[agent-workorders]] — never a silent "while I'm here."

