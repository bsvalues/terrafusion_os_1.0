# WO-AI-DISCOVERY-001b — GitHub `bsvalues` AI Estate Sweep

> **Goal:** TF-AI-OPS-001. **Status:** DISCOVERY ONLY — read-only enumeration. No recovery, no
> resurrection, no disposition.
> **Authorization:** explicitly authorized by the TF-AI-OPS-001 directive (per `AGENTS.md`,
> out-of-surface work requires explicit authorization; this is it).
> **Hard verification limit (recorded honestly):** this session's GitHub tooling is **scoped to
> `bsvalues/terrafusion_os_1.0` only**. Repository **metadata search works** across the account, but
> **file-level reads of other repos are denied** (`get_file_contents` → "repository … is not configured
> for this session"). Therefore every per-repo verdict below is **metadata-grade** (name / description /
> language / dates) — NOT file-verified. Deep AI/local/governed/proven verdicts require a session with
> broader repo scope, or the human.

## One-sentence truth

**The `bsvalues` GitHub account holds 130 repositories dominated by ~100 overlapping `Terra*`/`BCBS*`
property-valuation experiments (mostly Replit/StackBlitz/Manus single-shot generations) — this IS the
"scattered AI islands" problem the goal names; the canonical line is `terrafusion_os_1.0`, and only a
handful of repos are plausibly distinct, reusable AI capability — none verifiable from this session.**

## Estate shape (130 repos, fully enumerated)

| Bucket | Approx count | Examples | Recon verdict (metadata-grade) |
|---|---|---|---|
| **Canonical (this repo)** | 1 | `terrafusion_os_1.0` | the live governed line — all other discovery reconciles **to** this |
| **Modular split of the OS** (2025-10, coherent) | ~10 | `terrafusion-ai-platform` ("command brain, swarm intelligence, autonomous research"), `-os-core`, `-infrastructure`, `-shared`, `-ui-components`, `-government/-commercial/-government-platform`, `-marketplace`, `-docs`, `-developer-tools` | **candidate-for-reconciliation (unverified)** — a deliberate decomposition; relationship to the current monorepo unknown |
| **AI-named experiments** | ~12 | `TerraAgent` (Py), `terragroq` (Groq LLM), `PropertyTaxAI`, `TaxI_AI`, `TerraFusionAssistant` (Py), `AIDataConnect`, `PACS-DataBridge` ("AI-enhanced" PACS import), `TerraF` (Py), `TerraFusionTheory`, `BCBSDataEngine` (Py), `BCBSGeoAssessmentPro` (Py) | **island/unverified** — single-shot experiments; `PACS-DataBridge` flagged as relevant to the real Harris PACS lane |
| **Terra\* product/demo duplicates** | ~60 | `TerraFusionPro/Plus/Professional/Prof`, `TerraFusionMono`, `TerraFusionPlayground`, `TerraFusionSync`, `TerraBuild`, `TerraFlow`, `TerraGama`, `TerraPILT`, `mass-valuation-showcase`, `terrafusion-slco-demo`, `SHOCK_AND_AWE*` | **vapor-demo / stale-duplicate** — showcases, mockups, near-identical re-rolls |
| **BCBS\* county data/levy/GIS** | ~25 | `BCBSLevy`, `BCBSCOST`, `BCBSGISPRO`, `BCBSData/DataEngine`, `BSBCmaster`, `CountyDataSync-1` | **mostly non-AI data tooling** — relevant to county data, not the operator AI path |
| **Non-AI / scaffold / unrelated** | ~20 | `bsvalues` (profile), `thestart`, `sb1-*`, `Demon_Values`, `kid-safeguard-ai` (unrelated domain) | **not-AI / discard** |

## Highest-signal AI candidates (for 002 follow-up, all UNVERIFIED here)

1. **`terrafusion-ai-platform`** + the 2025-10 `terrafusion-*` family — the only coherent *modular* AI
   decomposition; its description names the exact concepts the live monorepo stubs ("command brain,
   swarm intelligence, autonomous research"). Worth a file-level check in a scoped session.
2. **`PACS-DataBridge`** — "AI-enhanced data import/export for PACS TrueAutomation." Touches the **real
   Harris PACS lane** (do-not-modify source), so high relevance + high caution.
3. **`TerraAgent`** / **`terragroq`** — agent + Groq-LLM experiments; local-vs-cloud unknown (Groq is a
   cloud LLM ⇒ likely **non-local** if central).

## Verification gaps (carried into 002 — do NOT treat as cleared)

- **No file-level verification of any non-canonical repo** (session scope limit). All buckets are
  name/description inferences.
- Public-vs-private: ~40 repos are **private**; even metadata is thin and contents are unreadable here.
- Recommended resolution: re-run 001b in a session whose GitHub scope includes the candidate repos
  (`terrafusion-ai-platform`, `PACS-DataBridge`, `TerraAgent`, `terragroq`, the `terrafusion-*` family),
  or have the human confirm which, if any, hold reusable **local** AI worth pulling onto the operator path.

## Doctrine read

The dominant finding is not a specific reusable asset — it is **sprawl**. 130 repos of overlapping
re-rolls is the islands problem in its purest form. The consolidation answer the goal already implies
holds: **one canonical governed path (`terrafusion_os_1.0` → TerraPilot/LocalOps)**, everything else
quarantined until a specific asset is proven local + governed + runtime-real. Nothing here changes that;
it quantifies it.

## Next in sequence

**WO-AI-DISCOVERY-001c** — external drives `D:`/`E:` sweep. **Not possible from this cloud container**
(no such drives mounted — a missing-private-file blocker for that sub-WO); must run where the drives are
mounted. Then **WO-AI-DISCOVERY-002** — unified reconciliation matrix folding 001a + 001b + 001c +
prior inventory.
