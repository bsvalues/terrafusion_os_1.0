{
  "checkedAt": "2026-04-30T22:52:53.929Z",
  "slice": "costforge-calibration-priority-contract",
  "status": "PASS",
  "decision": "COSTFORGE_CALIBRATION_PRIORITY_CONTRACT_REGISTERED_AND_ECHOED",
  "contractId": "costforge_calibration_priority_v1",
  "formula": {
    "score": "(max(0, COD - 15) * 2 + abs(medianRatio - 1.0) * 100 + abs(PRB) * 50) * sqrt(max(1, saleCount))",
    "criticalThreshold": 20,
    "watchThreshold": 5,
    "minSales": 3
  },
  "population": "CostForge county-scoped neighborhood calibration matrix rows with minimum sale-count filtering",
  "readPath": "GET /costforge/calibration/neighborhood-matrix?taxYear={taxYear}&minSales=3 plus GET /equity/deciles",
  "trustPosture": [
    "CostForge calibration priority is a cost-calibration triage contract, not County Studio correction priority.",
    "Requires county-scoped runtime and neighborhood matrix source data.",
    "Do not treat 39-county demo/reference data as production calibration proof."
  ],
  "checks": [
    {
      "id": "contract-registered",
      "passed": true,
      "proof": ["os-platform/core/pilot/terrafusion-suite-contracts.json"],
      "note": "CostForge calibration priority contract is present in the suite registry."
    },
    {
      "id": "contract-required",
      "passed": true,
      "proof": [
        "terraforge_operational_health_v1",
        "terraforge_statistics_compat_v1",
        "terraforge_segment_derivation_v1",
        "terraforge_correction_priority_v1",
        "costforge_calibration_priority_v1"
      ],
      "note": "Contract is part of the required suite contract list."
    },
    {
      "id": "triage-runtime-echo",
      "passed": true,
      "proof": [
        "frontend/apps/os-shell/src/pages/forge/cost/tabs/TriageTab.tsx"
      ],
      "note": "Triage runtime declares contract id, formula constants, thresholds, and UI contract echo."
    },
    {
      "id": "audit-adopted",
      "passed": true,
      "proof": ["os-platform/core/pilot/contract-adoption-audit.json"],
      "note": "Contract adoption audit treats CostForge triage as contract-backed instead of advisory/out-of-scope."
    }
  ],
  "failures": []
}
