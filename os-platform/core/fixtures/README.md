# Phase 8.5 Fixtures

Fixtures are deterministic inputs used by Phase 8.5 tests.

Rules:
- happy.json: minimum valid params + expected shape checks
- negative.json: one failure path (county mismatch or invalid params)
- No raw PII; use placeholders only
