apiVersion: v1
kind: ConfigMap
metadata:    
  name: {{ include "terrafusion-frontend.fullname" . }}-nginx
  labels:
    {{- include "terrafusion-frontend.labels" . | nindent 4 }}
data:
  default.conf: |
{{ .Values.nginx.serverBlock | indent 4 }}
