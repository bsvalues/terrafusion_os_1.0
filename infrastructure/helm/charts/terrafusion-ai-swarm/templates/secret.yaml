apiVersion: v1
kind: Secret
metadata:      
  name: {{ include "terrafusion-ai-swarm.fullname" . }}-secret
  labels:
    {{- include "terrafusion-ai-swarm.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secret }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
