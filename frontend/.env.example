VITE_API_BASE_URL=/api

# [Washington assessor launch]
# Vite dev/preview serves this credential-free public package through the same
# /launch-data/washington routes used by the production image. Both values are
# required together; leave both blank for truthful navigation-only fallback.
VITE_WASHINGTON_LAUNCH_MANIFEST_SHA256=
WASHINGTON_LAUNCH_DATA_SOURCE_URL=
