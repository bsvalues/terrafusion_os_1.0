import React from 'react';

/**
 * DcfPanel — Discounted Cash Flow Panel (stub)
 * Full implementation pending income approach backend endpoints.
 */
export const DcfPanel: React.FC = () => (
  <div className="rounded-lg border border-border bg-muted/30 p-4 text-sm text-muted-foreground">
    Discounted Cash Flow analysis available when income data is configured.
  </div>
);
