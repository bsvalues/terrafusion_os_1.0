# TerraFusion OS Shell

This folder is the canonical home for the OS shell UI. The current implementation still lives under `frontend/src`; when the UI is reorganized, migrate it here so all frontend apps sit under `frontend/apps/*`.

To run the shell during this transition, use:

```
pwsh ./tools/terra/terra.ps1 run frontend
```
