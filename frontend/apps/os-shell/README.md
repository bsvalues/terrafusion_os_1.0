# TerraFusion OS Shell

This folder is the canonical home for the OS shell UI. The app now lives here under `frontend/apps/os-shell/src` so all frontend apps sit under `frontend/apps/*`.

To run the shell during this transition, use:

```
pwsh ./tools/terra/terra.ps1 run frontend
```
