import type { Plugin } from 'vite';

// Temporary security plugin stub to keep the dev server running.
// Replace with the real implementation when available.
export function securityPlugin(): Plugin {
  return {
    name: 'security-plugin-stub',
  };
}
