'use client';

import React from 'react';

// Minimal component to test rendering without any dependencies
export const MinimalComponent: React.FC = () => {
  return (
    <div>
      <h1>TerraFusion Test</h1>
      <p>If you see this, basic rendering works!</p>
    </div>
  );
};

export default MinimalComponent;
