apiVersion: v1
kind: ConfigMap
metadata:      
  name: {{ include "terrafusion-backend.fullname" . }}-config
  labels:
    {{- include "terrafusion-backend.labels" . | nindent 4 }}
data:
  {{- with .Values.configMap }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
