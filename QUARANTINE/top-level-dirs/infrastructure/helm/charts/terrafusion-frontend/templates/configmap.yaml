apiVersion: v1
kind: ConfigMap
metadata:      
  name: {{ include "terrafusion-frontend.fullname" . }}-config
  labels:
    {{- include "terrafusion-frontend.labels" . | nindent 4 }}
data:
  {{- with .Values.configMap }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
