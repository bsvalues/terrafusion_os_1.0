apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "terrafusion-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "terrafusion-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "terrafusion-frontend.fullname" . }}:{{ .Values.service.port }}/health']
