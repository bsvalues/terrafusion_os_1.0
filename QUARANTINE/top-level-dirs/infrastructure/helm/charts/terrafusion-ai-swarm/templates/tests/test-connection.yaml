apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "terrafusion-ai-swarm.fullname" . }}-test-connection"
  labels:
    {{- include "terrafusion-ai-swarm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "terrafusion-ai-swarm.fullname" . }}:{{ .Values.service.port }}/health']
