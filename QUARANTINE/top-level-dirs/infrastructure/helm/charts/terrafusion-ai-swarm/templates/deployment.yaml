apiVersion: apps/v1
kind: Deployment
metadata:      
  name: {{ include "terrafusion-ai-swarm.fullname" . }}
  labels:
    {{- include "terrafusion-ai-swarm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "terrafusion-ai-swarm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "terrafusion-ai-swarm.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "terrafusion-ai-swarm.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: ai-swarm
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          envFrom:
            - configMapRef:      
                name: {{ include "terrafusion-ai-swarm.fullname" . }}-config
            - secretRef:      
                name: {{ include "terrafusion-ai-swarm.fullname" . }}-secret
          volumeMounts:
            - name: model-storage
              mountPath: {{ .Values.aiSwarm.modelPath }}
            - name: data-storage
              mountPath: {{ .Values.aiSwarm.dataPath }}
      volumes:
        - name: model-storage
          persistentVolumeClaim:
            claimName: {{ include "terrafusion-ai-swarm.fullname" . }}-model-pvc
        - name: data-storage
          persistentVolumeClaim:
            claimName: {{ include "terrafusion-ai-swarm.fullname" . }}-data-pvc
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
